import 'dart:ui';

class MenuItem {
  String title;
  String icon;
  Color color;
  Function func;
  bool numvisibility;
  MenuItem(this.title, this.icon, this.color, this.func, this.numvisibility);
}
